# Benchmark EDA

`benchmark_eda.ipynb` contains the shared loading, cleaning, and plotting
helpers for all benchmark graph datasets. The notebook reads CSV files from
`../data/graphs`, so it can be run after cloning the repository without local
copies of the tables.
